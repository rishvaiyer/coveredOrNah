name: Legacy app Pages deploy disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - name: Report legacy workflow status
        run: |
          echo "GitHub Pages deploys are handled by .github/workflows/pages.yml."
          echo "This legacy workflow remains only to prevent accidental duplicate Pages publishes."
